{
  "name": "page-audit",
  "version": "1.0.0",
  "description": "Audit a URL with Playwright + Chromium: status code, title, description, link/image counts, and basic security headers, output as JSON.",
  "main": "audit.js",
  "scripts": {
    "start": "node audit.js"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
